---
day: 117
week: 20
phase: "3 — Deep Learning & How LLMs Work"
title: "Build backprop in micrograd"
---

🎯 **Today:** follow Karpathy to code **automatic** backpropagation — the network computes its own gradients.

☀️ **Morning (~2h) — Learn**
- ▶️ Karpathy — implementing `.backward()` that propagates gradients through the whole expression.
- 📝 Note: each operation knows how to pass gradient to its inputs; do it in reverse order.

🌙 **Night (~2h) — Practice**
- Code along: add the backward step to your `Value` operations, then call `.backward()` and inspect the gradients.
- Commit: `Day 117: automatic backprop`.

✅ **Checklist**
- Implemented backward for operations
- Ran `.backward()` and saw gradients
- Committed to GitHub
- I built backprop, not just watched it

➡️ **Next:** a tiny net that learns.
