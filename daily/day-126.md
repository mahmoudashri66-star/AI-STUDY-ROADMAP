---
day: 126
week: 21
phase: "3 — Deep Learning & How LLMs Work"
title: "Rebuild your net in PyTorch"
---

🎯 **Today:** re-implement your hand-built network in PyTorch — bridging "I understand it" and "I can use the real tool".

☀️ **Morning (~2h) — Learn**
- ▶️ PyTorch "Build the Neural Network" (`torch.nn`) tutorial.
- 📝 Note: `nn.Linear` = a layer of neurons; `nn.ReLU` = activation; the optimizer does the weight updates.

🌙 **Night (~2h) — Build**
- Build a small `nn.Module` network, run a forward pass, compute a loss, and do a few training steps with an optimizer. Watch loss drop.
- Commit: `Day 126: net in PyTorch`.

✅ **Checklist**
- Built a network with torch.nn
- Trained it a few steps (loss drops)
- Committed to GitHub
- I can build nets in PyTorch AND explain what's underneath

🎉 End of Week 21 — you understand neural nets from the raw math up to the real framework. Next: how words become numbers (embeddings) — the doorway to LLMs.

➡️ **Next week:** embeddings — turning meaning into vectors.
