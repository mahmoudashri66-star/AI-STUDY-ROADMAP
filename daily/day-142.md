---
day: 142
week: 24
phase: "3 — Deep Learning & How LLMs Work"
title: "Tokens → embeddings (the LLM's input)"
---

🎯 **Today:** connect the pieces — each token is mapped to an embedding, and that's what actually enters the model.

☀️ **Morning (~2h) — Learn**
- ▶️ Review: text → tokens → token IDs → embeddings → into the network.
- 📝 Note: the "embedding layer" is just a lookup table: token ID → its vector.

🌙 **Night (~2h) — Practice**
- Draw the full input pipeline in `llm-input.md`: your sentence → tokens → IDs → embeddings. Label each step.
- Commit: `Day 142: tokens to embeddings`.

✅ **Checklist**
- Understood token → ID → embedding
- Drew the input pipeline
- Committed to GitHub
- Can explain what actually enters an LLM

➡️ **Next:** context window & length.
