---
day: 175
week: 30
phase: "4 — Building with LLMs (APIs, Prompting, RAG)"
title: "Structured output (JSON)"
---

🎯 **Today:** make the model return clean **JSON** your code can use directly — the key to building real apps (not just chat).

☀️ **Morning (~2h) — Learn**
- ▶️ **DeepLearning.AI — "Building Systems with the ChatGPT API"** (free) — structured output section.
- 📝 Note: ask for JSON with a specific shape; remember JSON ≈ Python dict (Day 23!).

🌙 **Night (~2h) — Practice**
- Prompt the model to extract `{name, topic, hours}` from a messy sentence and return JSON. Parse it in Python with `json.loads` and use the values.
- Commit: `Day 175: structured JSON output`.

✅ **Checklist**
- Got the model to return valid JSON
- Parsed + used it in code
- Committed to GitHub
- I can bridge LLM output → my program

➡️ **Next:** validating & repairing model output.
