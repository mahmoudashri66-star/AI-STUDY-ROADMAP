---
day: 224
week: 38
phase: "6 — Shipping Products + Architecture"
title: "Secrets & environment config"
---

🎯 **Today:** manage secrets the professional way — never in code, always in environment variables/secret stores (you did this for your bot!).

☀️ **Morning (~2h) — Learn**
- ▶️ Review `.env` files + hosting "secrets" settings; recall your Cloudflare bot secrets.
- 📝 Note: config that changes per environment (keys, URLs) lives outside the code.

🌙 **Night (~2h) — Practice**
- Refactor one of your apps to read ALL keys/config from environment variables (with a `.env.example`), and add `.env` to `.gitignore`.
- Commit: `Day 224: env & secrets`.

✅ **Checklist**
- Moved all secrets to env vars
- Added .env.example + .gitignore
- Committed to GitHub (no secrets!)
- I manage config like a pro

➡️ **Next:** deploy your app publicly.
